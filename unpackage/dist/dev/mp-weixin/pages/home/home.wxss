.home {
  position: relative;
}
.fixed {
  position: fixed;
  top: 44px;
  z-index: 100;
  background-color: rgba(255, 255, 255, 0.5);
}
.goods-list {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  padding: 16rpx;
}